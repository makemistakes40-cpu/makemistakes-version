# Server Environment
PORT=5001
NODE_ENV=development

# Database Connection
DATABASE_URL="postgresql://postgres:YOUR_DB_PASSWORD@localhost:5432/makemistakes?schema=public"

# Authentication Secrets (Generate secure random secrets in production)
JWT_ACCESS_SECRET="makemistakes_access_super_secret_key_12345"
JWT_REFRESH_SECRET="makemistakes_refresh_super_secret_key_67890"

# CORS Setup
CORS_ORIGIN="http://localhost:3000"
